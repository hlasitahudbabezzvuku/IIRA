// Test: Comparison operators
main(): int = {
    var a: bool = 1 < 2;
    var b: bool = 1 > 2;
    var c: bool = 1 <= 1;
    var d: bool = 1 >= 1;

    if (a && !b && c && d) {
        return 1;
    }

    return 0;
}
