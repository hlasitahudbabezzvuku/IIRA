// Test: Recursive fibonacci
fib(n: int): int = {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

main(): int = {
    var n: int = 10;
    var result: int = fib(n);
    $printf("Fibonacci(%d) = %d\n", n, result);
    return 0;
}
