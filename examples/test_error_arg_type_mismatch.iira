// Test: Argument type mismatch with method
Calculator: {
    add(self*, a: Int, b: Int): Int = {
        return a + b;
    }
}

main(): Int = {
    var calc: Calculator;
    var result: Int = calc.add(1, 2.5);  // Second arg is Float, should be Int - should error
    return result;
}
