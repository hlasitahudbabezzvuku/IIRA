// Test: Many method parameters
Calculator: {
    add(self*, a: Int, b: Int, c: Int, d: Int, e: Int): Int = {
        return a + b + c + d + e;
    }
}

main(): Int = {
    var calc: Calculator;
    return calc.add(1, 2, 3, 4, 5);
}
